(function($, window, document, undefined) {
    'use strict';

    // global vars
    var $window = $(window),
        $document = $(document);


    // doc events
    $document.on({


        /**
         * + DOM ready
         * =====================================================================
         */
        ready: function(ev) {
            console.log('DOM ready.');
        }

        /* = DOM ready */


    });


})(jQuery, window, document);
